import FavoritesClient from "@/components/FavoritesClient";

export const metadata = {
  title: "आवडत्या — आरती संग्रहालय",
};

export default function FavoritesPage() {
  return <FavoritesClient />;
}
